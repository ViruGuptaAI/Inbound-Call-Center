# .env
AZURE_VOICE_LIVE_API_KEY=<Find the key from your AI foundry resource>
AZURE_VOICE_LIVE_ENDPOINT=<Find the endpoint from your AI foundry resource>
VOICE_LIVE_MODEL=gpt-4o-mini
ACS_CONNECTION_STRING=<Find the connection string from your Communication Service resource>
ACS_DEV_TUNNEL=<Optional, only set it when you run local ACS test>

# BYO LLM (Bring Your Own Model) — Optional
# Set BYOM_PROFILE to route Voice Live to YOUR OWN Azure OpenAI deployment
# instead of the managed model. Leave empty to use the managed model (default).
# Options:
#   byom-azure-openai-chat-completion — your own Chat Completion model (GPT-4.1, GPT-4o, etc.)
#   byom-azure-openai-realtime        — your own Realtime model deployment
# When using BYOM, set VOICE_LIVE_MODEL to your Azure OpenAI deployment name.
BYOM_PROFILE=

# Ambient Scenes (Optional)
# Adds realistic background audio to simulate real-world call environments
# Options: none (default/disabled), office, call_center
# You can also add custom audio files - see README for details
# Example: AMBIENT_PRESET=call_center
AMBIENT_PRESET=none